pipeline {
    agent any 
    stages {
        stage('cloning') { 
            steps {
               echo "cloning"
                git branch: 'main', credentialsId: 'jenkinsTest', url: 'https://github.com/Oualitsen/optidial.git'
            }
        }

    }
}
